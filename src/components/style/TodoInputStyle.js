import React from "react";

const TodoInputStyle = (props) => {
  return (
    <div className="font-poppins text-center mt-10 mb-10 mx-auto">
      {props.children}
    </div>
  );
};

export default TodoInputStyle;
